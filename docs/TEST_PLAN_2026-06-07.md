# PriceCRE 测试验证文档
## 版本：2026-06-07 | 测试范围：全功能回归 + 鉴权重构 + AI 系统

---

## 测试准备

| 步骤 | 操作 |
|------|------|
| 1 | 打开浏览器隐身窗口 |
| 2 | 访问 https://pricecre.com |
| 3 | 确认页面正常加载（资产卡片列表可见） |

---

## 第一轮：未登录状态 (预计5分钟)

### 测试1 — 资产卡片按钮检查
| 步骤 | 操作 | 预期结果 |
|------|------|------|
| 1.1 | 点击任意资产卡片展开 | 卡片展开，显示"附属精算指标"区域为锁定状态 |
| 1.2 | 查看卡片右下角按钮 | 显示 **👤 需登录** 按钮（不显示"解锁"） |
| 1.3 | 点击 **👤 需登录** | 页面跳转到"我的"tab（登录页面） |
| 1.4 | 展开卡片，查看操作按钮区域 | 不应出现"AI 报告"和"AI 分析师"按钮 |

### 测试2 — 未登录解锁拦截
| 步骤 | 操作 | 预期结果 |
|------|------|------|
| 2.1 | 在地址栏按 F12 打开控制台 | 切换到 Console 标签 |
| 2.2 | 输入：`fetch('/api/assets/unlock', {method:'POST', headers:{'Content-Type':'application/json'}, body: JSON.stringify({propertyId:'test-123'})}).then(r => console.log('状态码:', r.status))` | 状态码显示 **401**（不是200） |
| 2.3 | 头部统计栏查看"已解锁资产" | 显示 **0** |

### 测试3 — 邀请弹窗
| 步骤 | 操作 | 预期结果 |
|------|------|------|
| 3.1 | 首次打开页面 | 可能出现邀请弹窗（正常） |
| 3.2 | 关闭弹窗或点击其他地方 | 弹窗消失 |
| 3.3 | 刷新页面（F5） | 弹窗**不再出现** |

---

## 第二轮：登录与注册 (预计5分钟)

### 测试4 — 测试账号登录
| 步骤 | 操作 | 预期结果 |
|------|------|------|
| 4.1 | 点击底部"我的"tab | 进入登录页面 |
| 4.2 | 点击 **微信登录（测试模式）** | 登录成功，显示"已登录"状态 |
| 4.3 | 查看头部"已解锁资产" | 显示之前解锁过的数量（首次为0） |

### 测试5 — 登录后按钮变化
| 步骤 | 操作 | 预期结果 |
|------|------|------|
| 5.1 | 展开任意资产卡片 | 右下角按钮变为 **🔓 解锁** |
| 5.2 | 操作按钮区域 | 出现"AI 报告"按钮（需解锁后才可点击） |

---

## 第三轮：解锁资产 (预计5分钟)

### 测试6 — 正常解锁资产
| 步骤 | 操作 | 预期结果 |
|------|------|------|
| 6.1 | 点击一张未解锁卡片的 **🔓 解锁** | 短暂显示"解锁中..."，然后卡片数据变为可见 |
| 6.2 | 查看头部"已解锁资产" | 数字 **+1**（实时变化） |
| 6.3 | 此时卡片上"AI 报告"和"AI 分析师"按钮 | 变为可用状态 |
| 6.4 | 再次点击同一张卡的解锁按钮 | 不应再扣费（后端跳过已解锁） |

### 测试7 — 解锁后数据可见
| 步骤 | 操作 | 预期结果 |
|------|------|------|
| 7.1 | 查看已解锁卡片的"附属精算指标" | 所有字段显示真实数值，不再显示 `*.**` |
| 7.2 | 刷新页面（F5） | 已解锁的卡片保持解锁状态 |

### 测试8 — 退出登录
| 步骤 | 操作 | 预期结果 |
|------|------|------|
| 8.1 | 点击"我的"→ 退出登录 | 退出成功 |
| 8.2 | 刷新页面 | 头部"已解锁资产"显示 **0** |
| 8.3 | 展开之前解锁过的卡片 | 重新显示为锁定状态（按钮变回"👤 需登录"） |

---

## 第四轮：AI 对话系统 (预计5分钟)

### 测试9 — AI 对话框 UI
| 步骤 | 操作 | 预期结果 |
|------|------|------|
| 9.1 | 登录后，展开一张**已解锁**的卡片 | — |
| 9.2 | 点击 **AI 分析师** 按钮 | 弹出 AI 对话框 |
| 9.3 | 检查头像 | 蓝紫渐变圆形头像，中间有星光轨道图标 |
| 9.4 | 检查标题 | 显示 **AI 分析师** |
| 9.5 | 检查欢迎消息 | 显示"嗨！我是 **[项目名称]** 的 AI 分析师 👋" |

### 测试10 — AI 对话额度显示（移动端）
| 步骤 | 操作 | 预期结果 |
|------|------|------|
| 10.1 | 手机模式打开 AI 对话框（全屏） | 对话框全屏显示 |
| 10.2 | 滑动到底部 | 在输入框上方可见：**AI对话额度** + 数字（如 `7/93`） |
| 10.3 | 额度条下方 | 有蓝色进度条 + "剩余 XX" 文字 |
| 10.4 | 额度条右侧 | "购买AI对话额度"按钮（点击跳转"我的"页面） |
| 10.5 | ⚠️ 确认 | 头部不应该再有第二个额度显示 |

### 测试11 — AI 对话额度显示（桌面端）
| 步骤 | 操作 | 预期结果 |
|------|------|------|
| 11.1 | 电脑模式打开 AI 对话框（右侧滑出） | 在输入框上方可见同样的额度条 + 购买按钮 |
| 11.2 | 与移动端一致 | 只有底部一个额度显示 |

### 测试12 — AI 对话功能
| 步骤 | 操作 | 预期结果 |
|------|------|------|
| 12.1 | 在输入框输入"你好，简单介绍自己" | — |
| 12.2 | 点击发送 | AI 回复"我是 PriceCRE 商业地产 AI 分析师..." |
| 12.3 | 再发送一条消息 | AI 正常回复 |
| 12.4 | 关闭对话框再打开 | 对话历史保留 |

### 测试13 — AI 对话额度消耗
| 步骤 | 操作 | 预期结果 |
|------|------|------|
| 13.1 | 记录当前额度显示（如 `7/93`） | — |
| 13.2 | 发送一条消息 | 已用数 +1（变为 `8/93`） |
| 13.3 | 关闭再打开对话框 | 额度保持更新后的值 |

---

## 第五轮：AI 报告 (预计3分钟)

### 测试14 — AI 精算报告
| 步骤 | 操作 | 预期结果 |
|------|------|------|
| 14.1 | 展开一张**已解锁**的卡片 | — |
| 14.2 | 点击 **AI 报告** 按钮 | 显示加载动画 → 生成分析报告 |
| 14.3 | 报告内容 | 包含评分（0-100）、优势、劣势、结论 |

### 测试15 — 纠错功能
| 步骤 | 操作 | 预期结果 |
|------|------|------|
| 15.1 | 在已解锁卡片的指标区域 | 每个字段右侧有**金色**纠错图标（非灰色） |
| 15.2 | 点击纠错图标 | 弹出输入框，可修改该字段的值 |
| 15.3 | 输入新值并确认 | 显示"纠错已提交" |

---

## 第六轮：我的页面 (预计5分钟)

### 测试16 — 页面布局
| 步骤 | 操作 | 预期结果 |
|------|------|------|
| 16.1 | 登录后点击"我的" | 页面显示（单列居中） |
| 16.2 | ⚠️ 确认 | 页面上**没有**旧的"Credit Panel"卡片 |
| 16.3 | 可见卡片 | 邀请好友 / 激活兑换码 / 商业付费 / AI对话 |

### 测试17 — 邀请链接
| 步骤 | 操作 | 预期结果 |
|------|------|------|
| 17.1 | 查看"邀请好友"卡片 | 输入框中显示 `pricecre.com/r/xxxxxx`（不是 sz2026） |
| 17.2 | 点击"复制"按钮 | 按钮文字变为绿色"已复制"，2秒后恢复 |
| 17.3 | 粘贴到记事本验证 | 链接格式正确，推荐码为用户真实推荐码 |

---

## 第七轮：后台管理 (预计3分钟)

### 测试18 — 菜单检查
| 步骤 | 操作 | 预期结果 |
|------|------|------|
| 18.1 | 访问 https://pricecre.com/admin | 进入后台 |
| 18.2 | 查看左侧菜单 | "邀请码"只出现**一次**（不是两次） |
| 18.3 | 菜单中有"AI报告"入口 | 点击可进入 AI 报告管理页 |

---

## 附录：API 接口验证（可选）

以下命令可在浏览器控制台（F12 → Console）中运行：

```javascript
// 1. 未登录解锁 — 必须返回 401
fetch('/api/assets/unlock', {
  method: 'POST',
  headers: {'Content-Type': 'application/json'},
  body: JSON.stringify({propertyId: 'test-123'})
}).then(r => r.json()).then(console.log)
// 预期: { error: "请先登录", requireLogin: true }

// 2. AI 对话 — 登录后可正常调用
fetch('/api/ai/chat', {
  method: 'POST',
  headers: {'Content-Type': 'application/json'},
  body: JSON.stringify({
    email: 'test@pricecre.com',
    messages: [{role: 'user', content: '你好'}],
    property: {projectName: '测试', city: '上海'}
  })
}).then(r => r.json()).then(console.log)
// 预期: { role: "assistant", content: "您好！..." }

// 3. 对话额度查询
fetch('/api/ai/chat-quota?email=test@pricecre.com')
  .then(r => r.json()).then(console.log)
// 预期: { tokens: 数字, totalUsed: 数字 }
```

---

## 优先级标记说明

| 标记 | 含义 |
|------|------|
| 无标记 | 核心功能，必须通过 |
| ⚠️ | 重点检查（之前有 Bug） |

---

**文档版本**: 2026-06-07
**测试环境**: https://pricecre.com  
**建议设备**: 桌面 Chrome + 手机 Chrome 各测一轮
