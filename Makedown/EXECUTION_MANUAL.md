# 🏆 pricecre.com 一期 Greenfield 战役：产品经理无脑复制执行手册
*(版本：v3.9.0-MAX 终极全局不截断版)*

---

## 🎯 关卡一：锁死底层数据底座与 47 项顶配契约（耗时 2 分钟）

### 💡 执行动作
请将以下框内的全部内容一次性全部复制，直接发给 Codex 建立数据底座意识：

```markdown
Codex，你现在是我的资深全栈总架构师。我们要对项目 pricecre.com 进行 Greenfield 从零重构，Scope 严格锁死为第一期“租金数据核心确权大闭环”。整个终端全量对齐 BitMart 金融级高频交易终端美学（黑曜石蓝黑 #0B0E11、极光绿 #00C570、霓虹红 #F24957）。

我是项目的产品经理，我不懂技术，所以我要求你执行的每一步都必须是 100% 完整、可直接运行的生产级源码，严格禁止使用任何 “// ...” 或 “// 保持不变” 等省略注释！

现在，请通读并死锁 /prisma/schema.prisma 配置文件以及 /types/indicators.ts 指标契约。

(产品经理：此处代码已在 ARCH.md 中完全提供，请在投喂给 Codex 时直接拼装发送)
```

---

## 🎯 关卡二：直出 Agent 批量并网安全上行 API（耗时 3 分钟）

### 💡 执行动作
直接将以下指令复制砸给 Codex，强制其输出大一统批量 Upsert 接口：

```markdown
底座已经锁死。现在进行第二步，请直接帮我编写本地自建 AI 抓取清洗 Agent 与线上对接的批量并网大一统上行 API 路由。

请严格根据以下产品规格和架构钢钉输出代码：
1. 接口文件路径硬编码绑定为：/app/api/agent/v1/bulk-upsert/route.ts
2. 安全红线防爆破：必须从 process.env.AGENT_SYNC_TOKEN 中校验 Bearer Token 鉴权，不匹配直接打回 401。
3. 流量控制锁：限制单次批量接收的最大 items 长度为 100 条。
4. 乱序时序拦截防御：读取 Agent 上传的 item.agentTimestamp 高精度时间戳，如果发现其小于或等于线上库内已有数据的 agentUpdatedAt 记录，直接判定为乱序重试流，无感跳过。

【最高交付红线】：禁止带有任何 “// ...” 的偷懒和截断。全量直出生产级可用的 TypeScript 完整代码！
```

---

## 🎯 关卡三：编写扣额打码原子动作 Server Action 盾牌（耗时 3 分钟）

### 💡 执行动作
直接将以下指令复制砸给 Codex，逼其吐出连接前后端最硬核的控额事务：

```markdown
非常好，并网接口极其规整。现在请帮我编写连接前后端的核心盾牌——用于处理用户前台点击解锁时的全栈服务端原子动作（Server Action）。

请严格根据以下商业产品红线规范和安全规范输出代码：
1. 文件路径硬编码绑定为：/lib/actions/unlock.ts
2. 零信任前置鉴权：必须使用 getServerSession 进行 NextAuth 登录凭证拦截。
3. 账户漏洞隔离熔断：如果查询到 user.password === null 且无微信唯一 OpenID 凭证，必须强制熔断打回。
4. 24小时去重锁逻辑：如果判定用户在 24 小时内曾经成功解锁过该资产点位，执行 0 额度消耗，以 CACHE_HIT 模式快速直出。
5. 双池分池按序扣减：优先扣除裂变赠额池（referralViewCount），赠额池枯竭归 0 后，才允许扣除付费池。
6. 服务端物理打码脱敏：确权成功后，一期项目仅允许向 UnlockedData 中打包释放 netEffectiveRent 指标，其余任何未解锁或二期高阶指标必须在 payload 响应体中通过服务端逻辑物理重写为 null，彻底断绝黑客通过 F12 浏览器控制台偷看或破解。
7. 【最高红线】：不准偷懒，禁止省略，100% 全量直出完整源码！
```

---

## 🎯 关卡四：攻克 BitMart 金融级前端核心自适应卡片组件（耗时 4 分钟）

### 💡 执行动作
直接将以下指令复制砸给 Codex，直出最高规格的前端 React 卡片流：

```markdown
完美的 Action 逻辑，后端防线全部焊死。现在我们来攻克最后一个核心交战面：编写元数据驱动、自适应三大赛道的前台高性能卡片组件。

请严格还原 BitMart 金融大厂级高频交易终端美学规范写出全量代码：
1. 文件路径硬编码绑定为：/components/PropertyCard.tsx
2. 视觉美学规范：卡片大底色强制采用黑曜石蓝黑（#0B0E11）。上涨/已解锁状态字体变极光绿（#00C570），未解锁拦截状态外挂精细的霓虹红（#F24957）LOCKED 徽章。
3. 双字体完美混排红线：中文标签、项目案名及地址描述强制绑定 Tailwind 类的 font-sans 类名；所有涉及到租金金额、百分比、数字指标强制必须单独包裹在 font-mono 类名下，干掉行高闪烁不平齐现象。
4. 状态机收拢与算力缓存优化：组件状态严禁使用零散脆弱的单 useState，必须使用标准的 useReducer。同时，利用 useMemo 固化 activeFields 的多赛道动态显隐过滤与 .sort() 排序，彻底封死二期上架 45 个衍生财务和人口指标时引起的前台运行时计算死卡假死。
5. 响应式防样式挤压：附属次要指标网格强制绑死 Tailwind 媒体查询 grid-cols-1 sm:grid-cols-2 lg:grid-cols-3，严防小屏手机发生样式重叠。
6. 【最高交付红线】：不准带有任何 “// 保持不变” 的省略，全量轰鸣吐出生产级源码！
```
